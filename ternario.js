const idadeMinima = 18;
const idadeCliente = 16;

// if (idadeCliente >= idadeMinima) {
//     console.log("cerveja");
// } else {
//     console.log("suco");
// }

// esttura ternário condição de existência, depois ? para começar, depois saída de true, : para separar, depois saída para falso

console.log(idadeCliente >= idadeMinima ? "cerveja" : "suco")